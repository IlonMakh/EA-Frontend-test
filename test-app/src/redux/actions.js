import { createAction } from '@reduxjs/toolkit';

export const SET_POPUP_STATE = createAction('setPopupState');
export const SET_RESPONSE_STATUS = createAction('setResponseStatus');
export const SET_BLOCK_STATE = createAction('setBlockState');
